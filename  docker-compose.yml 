version: "3.8"

services:
  flask-app:
    build: .
    ports:
      - "5005:5005"
    environment:
      - FLASK_ENV=production
      - FLASK_DEBUG=False
    depends_on:
      - redis

  redis:
    image: "redis:alpine"
    restart: always
